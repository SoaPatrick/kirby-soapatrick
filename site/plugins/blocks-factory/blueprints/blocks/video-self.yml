name: Video
icon: video
fields:
  video:
    type: files
    multiple: false
    query: page.videos
    uploads:
      template: blocks/video
  caption:
    label: field.blocks.video.caption
    type: writer
    inline: true
  align:
    label: Alignment
    type: toggles
    default: "default"
    options:
      default: Default
      wide: Wide