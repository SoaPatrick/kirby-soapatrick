name: Video
icon: video
fields:
  align:
    label: Alignment
    type: radio
    columns: 2
    default: "default"
    options:
      default: Default
      wide: Wide
  video:
    type: files
    multiple: false
    query: page.videos
    uploads:
      template: blocks/video
  caption:
    label: field.blocks.video.caption
    type: writer
    inline: true