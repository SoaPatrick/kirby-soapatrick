type: pages
headline: Box
parent: kirby.page("box")
info: "{{ page.published }}"
template: article
empty: No article yet
sortBy: published desc
search: true
layout: table
columns:
  format:
    label: Format
    type: tags
  category:
    label: Category
    type: tags
  tags:
    label: Tags
    type: tags
  project:
    label: Project
    type: category
  published:
    label: published
    align: right
    value: "{{ page.published.toDate('d.m.Y') }}"